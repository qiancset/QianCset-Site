/** @type {import('next').NextConfig} */
const nextConfig = {
/*     //要启用 静态导出 请更改输出模式
    //output: 'export',
    //可选：向所有路径 “/about”->“/about/” 添加尾部斜杠
    //尾随 "/" ：true
    //可选：将输出目录 “out” 更改为> “dist”
    //distDir:'dist' */
}

module.exports = nextConfig
